Mosdepth2csv is a script designed to read the "thresholds.bed.gz" files generated by mosdepth using the --threshold parameter and save them as CSV files. The CSV files are then compressed into a zip file.
